# AI開発ガイド

## 目的
- このリポジトリは社内向け食レポアプリのフロントエンドです。
- 現時点ではモック/静的データを使い、実バックエンドAPIは呼び出しません。
- レイアウト、UX、コンポーネント構成に集中します。
- 将来的に実際に動くアプリに発展させる想定です。

## 技術スタック
- Next.js（App Router）
- npm
- TypeScript

## 作業ルール
- Issueごとにブランチを切る。
- PRを必ず作成または更新する。
- 変更範囲はIssueの内容に限定する。
- 以降のやり取りは日本語で行う。

## 完了条件
- `npm run build` が通ること。
- Preview が動くこと（Vercel Preview）。
- PR本文に確認ポイントを書くこと。
- Issueをクローズしてよい最終PRのみ `Closes #<issue>` を書く。
- 関連付けだけなら `Refs #<issue>` を使う。

## UIモックのルール
- 必要に応じて空/ローディング/エラー状態を作る。
- モックデータは `app/_mock` 配下にまとめる。
- 実ネットワーク通信は行わない。
